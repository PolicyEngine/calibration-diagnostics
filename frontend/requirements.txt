# Pinned to the policyengine-us version the current populace release was built
# with (build_manifest -> 1.752.2). A mismatch silently shifts weighted totals
# (uprating/weighting logic changes between versions), which would make the
# hosted variable lookup disagree with the calibration for the same release.
# Bump this together with the release when a new build ships.
policyengine-us==1.752.2
huggingface_hub>=0.25,<1.0
numpy>=1.26,<3.0
